import React, { useState } from "react";
import ReactDOM from "react-dom";

const ListEpisodios = () => {
  return <li className="dropdown-item ">  {/* {ep.name}   */}lista de episodiso</li>;
};

export default ListEpisodios;
